# ═══════════════════════════════════════════════════════════
#  Inventario de Ansible - PetStore
# ═══════════════════════════════════════════════════════════

[vps]
# Servidor VPS donde se despliegan las 3 webs con Docker Compose
# Sustituir TU_IP_VPS por la IP real del servidor
194.163.138.9 ansible_user=root ansible_port=22760 ansible_ssh_private_key_file=~/.ssh/id_ed25519

[clientes]
# Sistemas ya existentes donde desplegar solo el microservicio
# (sin Docker, instalación nativa con systemd)
# Descomenta y añade las IPs de los sistemas de clientes:
# cliente1 ansible_host=192.168.1.10 ansible_user=deploy
# cliente2 ansible_host=192.168.1.20 ansible_user=deploy

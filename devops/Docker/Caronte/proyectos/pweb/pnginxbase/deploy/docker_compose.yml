services:
  web:
    build: 
      context: ${CONTEXTO}
      #args: #variables en dockerfile para la construcción de la imagen
      #  - USUARIO=pedro
      dockerfile: ./dockerfiles/sweb/nginx/Dockerfile
    container_name: ${NOMBRE}
    image: peedroogzz/ubbase:latest
    ports:
      - "${PORT_WEB}:80"
    env_file:
      - ./.env
    volumes:
      - ./logs:/root/logs
    networks:
      netcarontewebnginx:
          ipv4_address: ${IP}

networks:
  netcarontewebnginx:
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}
# Contexto de build: raíz de Caronte
CONTEXTO=../../../

# Nombre del proyecto (se pasa como ARG al Dockerfile)
PROYECTO=pnest

# Nombre del contenedor
NOMBRE=api-pokemon

# Puerto del host → puerto del contenedor (3001)
PORT_WEB=3001

# ── Conexión a PostgreSQL (contenedor ctPostgres) ──────────────
DB_HOST=172.120.60.10
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres123
DB_NAME=hlcdb

# Red interna propia
IP=172.20.0.10
SUBNET=172.20.0.0/24

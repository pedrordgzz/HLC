apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-portfolio-con-helm.fullname" . }}-test-connection"
  labels:
    {{- include "mi-portfolio-con-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi-portfolio-con-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
